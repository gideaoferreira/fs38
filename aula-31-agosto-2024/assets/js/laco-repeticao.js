// = : Atribuição
// == : Comparação de valor
// === : Comparação de tipo e valor
// != : Diferete
// < > = Menor e Maior que.
// Condicional E (&&)
// Condicional OU (||)

// User, Editor, Admin, Visitante

let typeUser = "Admin";

if (name === "User") {
  //....
} else {
}

for (let x = 1; x <= 10; x++) {
  if (x >= 2 || x < 10) {
    console.log("IF", x);
  } else {
    console.log("ELSE", x);
  }
}
